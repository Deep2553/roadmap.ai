node_modules
.next
.git
.github
.vercel
sqlite.db
sqlite.e2e.db
*.db
.env
.env.local
.env*.local
npm-debug.log*
README.md
docs
infra
# tests/ is kept in the build context: the Jenkins pipeline runs Vitest inside
# the builder stage. It never reaches the runner image, which copies only
# .next/standalone, .next/static and public.
e2e
playwright-report
test-results
